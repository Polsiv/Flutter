import 'package:flutter/material.dart';

class BackgroundNuevo extends StatelessWidget {
  const BackgroundNuevo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}